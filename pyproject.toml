[project]
name = "main"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "a",
]

[tool.uv.sources]
a = { git = "https://github.com/alfman99/private-repo-a.git" }
